# Open Electron window with hot-reload
dev:
    npm run dev

# Build main + preload + renderer
build:
    npm run build

# Preview production build
preview:
    npm run preview

# Package into platform installers
package:
    npm run package

# Lint
lint:
    npm run lint

lint-fix:
    npm run lint:fix

# Format
format:
    npm run format

format-check:
    npm run format:check

# Install dependencies
install:
    npm ci
